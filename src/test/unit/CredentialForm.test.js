import CredentialForm from '../../components/CredentialForm'

describe('CredentialForm', () => {
    describe('#fields', () => {
        xit('test', () => {
        })
    })

    describe('#buttons', () => {
        xit('test', () => {
        })
    })

    describe('#button', () => {
        xit('test', () => {
        })
    })
})
